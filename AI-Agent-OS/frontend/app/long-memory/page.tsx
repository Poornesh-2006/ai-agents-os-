import MemoryPage from "@/components/MemoryPage";

export default function LongMemoryPage() {
  return (
    <MemoryPage
      title="Long-Term Memory"
      subtitle="Permanent project rules, known fixes, model settings, and architecture decisions."
      endpoint="/memory/long-term"
    />
  );
}

